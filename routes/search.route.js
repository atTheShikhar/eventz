const express = require('express');
const { searchEventsController } = require('../controllers/search.controller');
const router = express.Router();

// router.get('/search/events',searchEventsController);

module.exports = router;